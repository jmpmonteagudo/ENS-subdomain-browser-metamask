# ENS-subdomain-browser-metamask

An example of how to:

- Create new subdomains using the FIFS registrar in Rosten network
- Assign a resolver
- Set an address for the subdomain

from the browser using Metamask.

